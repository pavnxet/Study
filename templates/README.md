# Templates

This directory contains lightweight templates for repeatable archive additions. Templates must describe structure and metadata fields; they must never contain fabricated academic documents.
